{
  "extends": "@repo/typescript-config/base.json",
  "compilerOptions": {
    "outDir": "./dist",

    "baseUrl": ".", // This allows relative imports to start from the root
    "paths": {
      "@repo/common/*": ["packages/common/src/*"] // Path alias for your UI package
    }
  }
}
